{
  "name": "aventario-website",
  "version": "1.0.0",
  "private": true,
  "description": "Aventario corporate website — static HTML, no build step.",
  "scripts": {
    "build": "echo 'Static site — no build needed.'"
  }
}
